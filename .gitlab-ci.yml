image: maven:3.8-openjdk17
default:
  tags:
    - macJumanji
stages:
  - test
Integration test with allure:
  stage: test
  script:
    - mvn clean test
    - mvn allure:report
  artifacts:
    paths:
      - target/allure-results