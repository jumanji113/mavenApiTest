image: maven:3.8-openjdk-11
default:
  tags:
    - macJumanji
stages:
  - test
Integration test with allure:
  stage: test
  script:
    - java -version
    - mvn -v
    - mvn clean test
    - mvn allure:report
  artifacts:
    paths:
      - target/allure-results